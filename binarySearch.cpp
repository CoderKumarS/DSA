#include<iostream>
using namespace std;
int main(int argc, char const *argv[])
{
    int beg = 0, end, mid, a[100], n;
    cout<<"Enter the size of array";
    cin>>n;
    for (int i = 0; i < n; i++)
    {
        cin>>a[i];
    }
    end = n;
    do
    {
        /* code */
    } while (beg<=end);
    
    return 0;
}
